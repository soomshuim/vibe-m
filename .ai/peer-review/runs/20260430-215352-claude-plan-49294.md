# Peer Agent Review

| Field | Value |
|---|---|
| Target | claude |
| Mode | plan |
| Project | wavvy |
| Repo | /Users/zen/Project/wavvy |
| Git repo | yes |
| Branch | master |
| Created | 2026-04-30 21:58:54 KST |
| Exit code | 0 |

## Request

Review this implementation plan for decision completeness, feasibility, risks, and acceptance criteria. User wants implementation only if Claude review PASS.

Plan: Wavvy Upload Finalization Harness

Summary:
- Add `python3 wavvy.py finalize-upload <series>`.
- During production, `input/tracks/*.txt` remains SSOT for Style/Exclude/Lyrics.
- At upload FINAL time, `finalize-upload` reads txt sources plus `output/report.json`, writes full track sources into `concept.md`, verifies completeness, then deletes txt sources only if concept archive is complete.
- For current `SERIES/20-00`, restore deleted txt sources from git commit `b6f13c4^`, align with current WAV order/report, merge into concept, then delete txt after contract PASS.

Key implementation details:
- Add parsing helpers in `wavvy.py` for txt track sources with required fields: Track, Order, Type, BPM, Vocal, sections `=== STYLE ===`, `=== EXCLUDE ===`, `=== LYRICS ===`.
- Add report timestamp helper using report.json durations and repeat=2/fade=0.8 metadata where available.
- Add `## Final Track Sources` section in concept.md. Replace existing section if present. Each track records Timestamp, Repeat timestamp if repeat=2, Filename, Type, BPM, Vocal, SHA-256, STYLE, EXCLUDE, LYRICS.
- Add source contract validation used by `validate`, `pack`, and `finalize-upload`:
  - If txt files exist, count must match audio files and every txt must have required metadata/sections/non-empty lyrics.
  - If txt files are absent, concept.md must contain complete Final Track Sources for every audio/report track.
  - validate reports source contract as PASS/FAIL. pack fails if neither txt nor complete Final Track Sources exist.
- Add `--check` dry run to `finalize-upload`, and `--keep-txt` optional safety switch. Default behavior deletes txt after successful concept archive merge.
- Update `MASTER/WORKFLOWS.md` and `MASTER/youtube/YOUTUBE.md` to document staged SSOT transition and finalize-upload requirement.

20-00 migration:
- Use git history restore from `b6f13c4^` for the 20 deleted txt files.
- Rename/reorder txt files to current WAV order if needed:
  01 Paycheck, 02 Night Rider, 03 Bottom to the Top, 04 Yang Gang, 05 Old Cassette, 06 Real Talk, 07 LLC, 08 Boomerang, 09 Black Mirror, 10 Bottom Line, 11 Overtime Flame, 12 Small Talk, 13 Concrete, 14 Cold Stack, 15 Old Page, 16 Rewrite, 17 Fake, 18 Engine, 19 Side Street, 20 Slow Glow.
- Run finalize-upload to archive full sources into concept and delete txt only after contract PASS.

Tests:
- `python3 wavvy.py finalize-upload SERIES/20-00 --check` PASS.
- `python3 wavvy.py finalize-upload SERIES/20-00` PASS.
- `python3 wavvy.py validate SERIES/20-00` PASS with source contract from concept archive after txt deletion.
- Regression by temporary copied fixture or manual dry run: missing txt + no Final Track Sources fails; missing LYRICS fails; count mismatch fails.
- `python3 -m py_compile wavvy.py` PASS.

## Context

### Git Status

```
(clean)
```

### Diff Stat

```
(no unstaged diff stat)
```

## Peer Result



### Peer Review Verdict
- **Result: FAIL**
- **Confidence: high**

### Findings

| Severity | File/Area | Issue | Recommendation |
|---|---|---|---|
| **CRITICAL** | txt parser | Plan requires `=== EXCLUDE ===` as mandatory section, but verified via git history that multiple 20-00 txt files (Black Mirror, Cold Stack, and likely others) have **no EXCLUDE section**. Parser would reject valid source files. | Make EXCLUDE optional. Only STYLE and LYRICS should be required sections. |
| **CRITICAL** | `validate` / `pack` | Source contract validation added globally to `validate` and `pack` would break **all 13 existing series**. Series 04-00 through 22-00 exist; most lack both txt files and Final Track Sources. Series 11-00 txt uses a completely different markdown format (`# Track 01`, `## Lyrics`). Series 12-00 txt has no `Order` or `Vocal` fields. A single parser with fixed required fields cannot work across series. | Scope source contract to `finalize-upload` only, OR make it opt-in (e.g., only enforce when a `## Final Track Sources` section or txt files are explicitly present). Do NOT gate `validate`/`pack` on this for all series. |
| **HIGH** | Archive format | Plan's required txt fields list (`Track, Order, Type, BPM, Vocal`) omits `Key` and `Length`, which are present in actual txt files. Plan's concept archive format also omits `Key`, `Length`, `Genre/Mood` (from WAV filename), and the `META` section (RUBRIC matching, PASS date, 8-Factor score, operations rule). Verified: Paycheck.txt has `Key: Minor`, `Length: 4:30`, and full META block. | Include Key, Length in parsed fields. Archive META section content (at minimum RUBRIC match and PASS date). Include Genre/Mood from WAV filename or report.json. |
| **HIGH** | txt Order mismatch | Paycheck.txt has `Order: 04`, Rewrite.txt has `Order: 11`, but current WAV order is Paycheck=01, Rewrite=16. Plan says "rename/reorder txt files to current WAV order" but doesn't specify the matching algorithm. Matching by old Order number would produce wrong mappings. | Specify: match restored txt files to WAV files by **title** (case-insensitive, ignoring parentheticals for LLC). Ignore old Order field values entirely. |
| **HIGH** | Timestamp redundancy | concept.md already contains calculated timestamps at lines 33-50 (e.g., `00:00 - 01. Paycheck`, `02:31 - 02. Night Rider`). Plan proposes recalculating timestamps from report.json for the Final Track Sources section. Unclear whether these supplement, replace, or must match the existing timestamps. | Clarify relationship. If Final Track Sources timestamps serve a different purpose (e.g., including repeat-pass offsets), state this explicitly. If they duplicate existing timestamps, reuse the existing data. |
| **MEDIUM** | Test plan | "Temporary copied fixture or manual dry run" is not reproducible. No concrete edge-case test commands specified: what happens with 0 txt files + 0 Final Track Sources? What happens with partial txt set (15 of 20)? What about a txt file with empty LYRICS? | Specify concrete test scenarios with expected exit codes. At minimum: (1) happy path `--check`, (2) missing LYRICS fails, (3) count mismatch fails, (4) concept-only validation after txt deletion succeeds. |
| **MEDIUM** | Rollback | No recovery strategy if `finalize-upload` writes malformed data to concept.md. The only implicit safety net is `git checkout -- concept.md`, but this isn't documented. | Document: `--check` first (already planned), and note that `git checkout` is the recovery path. Consider writing to a temp file and atomically replacing. |
| **LOW** | EXCLUDE section | Even if made optional in parser, the plan should document that EXCLUDE content may be empty/absent for some tracks in the Final Track Sources archive, so downstream consumers don't assume it's always present. | Add a note in the archive format spec that EXCLUDE is optional. |

### Test and Acceptance Notes

- **Not yet runnable** — no code exists yet, so no tests can be executed.
- **Missing test**: backward compatibility — run `python3 wavvy.py validate SERIES/11-00` and `SERIES/12-00` after changes to confirm they still PASS.
- **Missing test**: partial txt coverage — what if only 15 of 20 txt files are restored from git? Plan says count must match audio files, but restore could fail for individual files.
- **Missing test**: `finalize-upload` idempotency — running it twice should either no-op or cleanly re-archive.
- **Confirmed feasible**: git restore via `git show b6f13c4^:SERIES/20-00/input/tracks/Paycheck.txt` works and returns valid content.
- **Confirmed feasible**: report.json contains all fields needed for timestamp calculation (durations, processing_params.fade, processing_params.repeat).
- **Confirmed feasible**: SHA-256 hashes available in report.json for all 20 tracks.

### Follow-Up for Requesting Agent

1. **Fix the two CRITICAL findings before proceeding**:
   - Make `=== EXCLUDE ===` optional (not a required section).
   - Scope source contract validation to `finalize-upload` only, or make it opt-in for `validate`/`pack` (e.g., only enforce when txt files or Final Track Sources are detected). Do not break the 12 other series.

2. **Address HIGH findings**:
   - Add Key, Length, and META to the parsed/archived fields.
   - Specify title-based matching (not Order-based) for txt-to-WAV mapping during 20-00 migration.
   - Clarify timestamp relationship with existing concept.md track list.

3. **Strengthen test plan** with concrete scenarios and expected outcomes, including backward-compatibility checks against series 11-00 and 12-00.

4. **Re-submit the revised plan** for re-review after addressing the above.

