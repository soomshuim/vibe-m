# Peer Agent Review

| Field | Value |
|---|---|
| Target | claude |
| Mode | review |
| Project | wavvy |
| Repo | /Users/zen/Project/wavvy |
| Git repo | yes |
| Branch | master |
| Created | 2026-05-02 00:53:40 KST |
| Exit code | 0 |

## Request

Review the implemented Wavvy harness changes against .ai/plans/PLAN_wavvy_harness_setting.md.

Scope:
- wavvy.py
- wavvy_harness/*
- tests/test_harness.py
- .ai/state.json
- MASTER/SSOT.md
- SERIES/20-00/concept.md
- CLAUDE.md and MASTER/cli/SPEC.md
- analysis/plan/review artifacts

Focus on bugs, behavioral regressions, missing acceptance checks, bad phase semantics, incorrect JSON outputs, and whether the source-final/upload-ready gates behave correctly. Treat this as read-only QA. Return PASS/FAIL/NEEDS_USER_DECISION.

## Context

### Git Status

```
 M .ai/HANDOFF.md
 M CLAUDE.md
 M MASTER/cli/SPEC.md
 M SERIES/20-00/concept.md
 M wavvy.py
?? .ai/peer-review/runs/20260502-002626-claude-review-40431.md
?? .ai/peer-review/runs/20260502-003021-claude-review-49474.md
?? .ai/peer-review/runs/20260502-003357-claude-plan-57722.md
?? .ai/plans/PLAN_wavvy_harness_setting.md
?? .ai/state.json
?? MASTER/SSOT.md
?? meetings/2026-05-02_wavvy-harness-engineering-analysis.md
?? tests/
?? wavvy_harness/
```

### Diff Stat

```
 .ai/HANDOFF.md          |  22 +++++++
 CLAUDE.md               |   5 +-
 MASTER/cli/SPEC.md      |   7 +-
 SERIES/20-00/concept.md |  53 ++++++---------
 wavvy.py                | 169 +++++++++++++++++++++++++++++++++++++++++-------
 5 files changed, 200 insertions(+), 56 deletions(-)
```

## Peer Result

Now I have a comprehensive view. Let me finalize the verdict.

---

# Peer Review Verdict
- **Result: PASS**
- **Confidence: high**

The implementation faithfully translates `PLAN_wavvy_harness_setting.md` into working code. WP1–WP7 (the wavvy-repo scope) are landed: helper package (`wavvy_harness/{state,doctor,gate}.py`), CLI commands (`doctor`, `state`, `gate`), P0 fixes (repeat-aware `crossfade_reduction`, writer return checks, banner/.mkv/--use-xfade drift), unit tests (`tests/test_harness.py`), state contract (`.ai/state.json` revision 1, source_final), SSOT (`MASTER/SSOT.md`), and 20-00 concept cleanup. No critical bugs, no behavioral regressions to existing pipelines.

# Findings

| Severity | File/Area | Issue | Recommendation |
|---|---|---|---|
| Low | `wavvy_harness/gate.py:62-91` | Gate emits **duplicate but textually distinct blockers** when both `check_state` ("source_final requires output/report.json") and `run_gate` source_checks ("report_json_present") fire on the same missing artifact. `sorted(set(...))` only dedupes identical strings. | Either drop the redundant in-gate `source_checks` (state-level checks already cover them), or normalize messages so dedup works. Cosmetic — does not change PASS/FAIL. |
| Low | `wavvy.py:2009-2013` (gate_cmd) | `contextlib.redirect_stdout` only captures stdout from `validate_project`. `log_error` writes to stderr, so JSON output is not strictly clean if validation prints errors. Other helpers (`run_gate`, `check_state`) are pure and don't pollute. | If clean JSON is a contract, also redirect stderr or refactor `validate_project` to return errors without printing. Low-impact. |
| Low | `wavvy_harness/doctor.py:65-80` | `_check_writable_temp` writes into `<repo>/.ai/` directly. Plan WP3 said `.ai/tmp` or system temp. Functionally fine (`.ai/` exists and is writable in real use), but slightly off-spec. | Either use `.ai/tmp` (auto-create) or document the choice. Optional. |
| Low | `wavvy.py:2444-2451` (pack artifact writers) | Implementation runs all three writers and aggregates with `all([...])`, which is not strictly "fail fast" (writes #2/#3 still execute after #1 fails). Plan said "fail fast if any writer fails." Net effect (non-zero exit on any failure) matches plan; only the ordering differs. | Either short-circuit on first failure, or update the plan note. Cosmetic. |
| Low | `SERIES/20-00/output/report.json:16` | Existing on-disk report has the **pre-fix** `crossfade_reduction: -3922.52` (sum − final). Plan explicitly chose "keep historical reports as-is", so this is by design — but downstream tools or eyeballs may be misled until pack regenerates it. | Accept as plan-aligned. Optionally add a note in `provenance.md` or run `pack -y` later. |
| Info | WP8 (`claude-center/scripts/team-director-pipeline.sh`) | Cross-repo work is **out of scope for this review** (separate repo, no diff visible here). Cannot be verified from `/Users/zen/Project/wavvy`. | Run the cross-project gate from `/Users/zen/Project/claude-center` separately. |

# Verifications (positive checks)

- **`crossfade_reduction` fix** (`wavvy.py:1399-1410`): now `total_original_duration * repeat - final_duration`. Unit test `test_generate_report_crossfade_reduction_uses_repeat` asserts 30 × 2 − 58.4 = 1.6. ✅
- **Phase inference** (`state.py:96-110`): for 20-00 (final_mkv missing, FTS present, report tracks > 0, audio_files=20) → `source_final`. Matches `state.json` and concept.md. ✅
- **WP6 conflict rules**: rule 1 `_has_stale_final_todo`; rule 2 `source_final` blockers when FTS missing; rule 3 next_action mentions upload/render → warning for source_final, blocker for render/upload phases. All present. ✅
- **State writer** (`state.py:243-261`): `tempfile.NamedTemporaryFile` in same parent dir + `replace` (atomic rename), `--if-match` revision CAS, `revision` increments to 2 on next write. ✅
- **Doctor exit semantics**: required failures only count `status == "fail"` (lowercase) for blockers; disk-free <5GB returns `warn` (not blocker). ✅
- **Image-mode preview fix**: `wavvy.py:2196-2210` now branches into `render_video_from_image` when `paths.is_image_mode`. 20-00 will preview correctly. ✅
- **Spec drift cleanup**: `VIBEM` banners → `WAVVY`, `pack --use-xfade` instruction replaced, `final.mp4` → `final.mkv` consistent. ✅
- **Concept cleanup (WP7)**: Series Status now `phase: source_final`, table uses `PASS`/`FINAL` (no DRAFT), 미해결 section reframed around subtitle upload. 20 `### NN. Title` headings under Final Track Sources match report's 20 tracks. ✅
- **Acceptance negative gate**: `gate --stage upload-ready` correctly emits blockers for missing `final.mkv` and `upload.csv` (verified via code path; final_mkv/upload_csv absent on disk). ✅

# Test And Acceptance Notes

- Unit tests in `tests/test_harness.py` cover the three required cases per plan. The `from wavvy import TrackInfo, generate_report` at module level requires `pandas` and `ffmpeg-python` installed; that's already a project requirement (doctor checks). I did **not execute** the tests (read-only review).
- Recommended runs (read-only review did not perform them):
  - `python3 -m py_compile wavvy.py wavvy_harness/*.py`
  - `python3 -m unittest tests/test_harness.py`
  - `python3 wavvy.py doctor --json`
  - `python3 wavvy.py validate SERIES/20-00`
  - `python3 wavvy.py state SERIES/20-00 --check --json`
  - `python3 wavvy.py gate SERIES/20-00 --stage source-final --json` (expect PASS)
  - `python3 wavvy.py gate SERIES/20-00 --stage upload-ready --json` (expect FAIL with named blockers)
- WP8 cross-project gates (`bash scripts/team-director-pipeline.sh ...`) must be run from `/Users/zen/Project/claude-center`. Out of scope here.
- Coverage gap (Low): no test exercises `gate` directly or the SSOT conflict rule end-to-end through the CLI. Plan's eval-fixture follow-up (Low #1 from prior plan review) is still narrative-only.

# Follow-Up For Requesting Agent

1. Treat this as PASS. Run the acceptance commands (especially `unittest`, `state --check`, `gate --stage source-final`, and the negative `upload-ready` gate) before committing — read-only review did not execute them.
2. Optional polish (Low findings): drop duplicate `source_checks` in `gate.py` to clean up blocker output; align `_check_writable_temp` to `.ai/tmp` per plan WP3 wording; convert pack writer aggregation to short-circuit fail-fast.
3. Run WP8 acceptance gates from the **claude-center** working tree (not this repo) before declaring the cross-project harness shipped.
4. Commit wavvy-side changes; record HANDOFF (Codex → Claude peer review PASS) per project hook contract.

