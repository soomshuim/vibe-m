# Peer Agent Review

| Field | Value |
|---|---|
| Target | claude |
| Mode | review |
| Project | wavvy |
| Repo | /Users/zen/Project/wavvy |
| Git repo | yes |
| Branch | master |
| Created | 2026-05-02 00:29:11 KST |
| Exit code | 0 |

## Request

Review this Wavvy harness-engineering analysis as Claude Opus 4.7 xhigh/max equivalent.

Primary artifact:
- meetings/2026-05-02_wavvy-harness-engineering-analysis.md

Evaluate whether the analysis is correct, complete, and actionable for setting up a harness. Focus on missing risks, wrong priorities, implementation traps, and whether the proposed state/gate/doctor/cross-project pipeline is sufficient. Treat this as read-only review. Return PASS/FAIL/NEEDS_USER_DECISION with concrete findings.

## Context

### Git Status

```
?? meetings/2026-05-02_wavvy-harness-engineering-analysis.md
```

### Diff Stat

```
(no unstaged diff stat)
```

## Peer Result

### Peer Review Verdict
- Result: NEEDS_USER_DECISION
- Confidence: high

The analysis is largely accurate and the harness framing is sound, but several semantic conflicts and scope gaps will cause implementation churn unless the user resolves them before planning. Findings below are evidence-based, with file/line cites where I verified claims directly.

### Findings

| Severity | File/Area | Issue | Recommendation |
|---|---|---|---|
| High | analysis §Recommendation #4 (Final State semantics) | Tells gate to mark missing `output/final.mkv` as FAIL/BLOCKED, but the user treats 20-00 as FINAL even though `output/` only contains `provenance.md`, `report.json`, and subtitles (no `final.mkv`, no `upload.csv`). Without distinguishing "Source-Final" vs "Render-Final" vs "Upload-Final", every FINAL series will fail the gate the analysis defines. | Define explicit phase enum in `.ai/state.json` (e.g. `source_final`, `render_final`, `upload_final`) and have gate consult that phase before requiring `final.mkv`. Decide whether artifacts are committed or local-only — currently `final.mkv`/`upload.csv` look gitignored. |
| High | analysis §Recommendation #1 (state.json) vs CLAUDE.md hook contract | `.ai/state.json` is proposed as the resume target but the existing SessionStart hook reads `.ai/HANDOFF.md` (verified: today's session-start banner shows the HANDOFF entry). Demoting HANDOFF without updating the hook breaks cross-agent handoff for both Claude and Codex. | Add an explicit migration step: update `~/Project/claude-center` SessionStart/UserPromptSubmit hooks (or write a generator that emits HANDOFF.md from state.json) before HANDOFF is demoted. The analysis does not mention this dependency. |
| High | analysis §AI Ops "stale TODO 오염" + "active state" | `.ai/state.json` has no schema, no writer ownership rule, no concurrency story. Two agents (Claude / Codex) writing simultaneously is a real risk on this project given the existing handoff pattern. | Plan must include: minimal schema (with required fields), single-writer rule (e.g. `wavvy.py state` only), and atomic write semantics (tmp + rename or git pre-commit guard). Otherwise this becomes another append-only log. |
| Medium | analysis §Engineering Lead bullet "report.json crossfade_reduction = -3922.52" | Verified: `SERIES/20-00/output/report.json` shows `final_duration=7876.24`, `total_original_duration=3953.72`, `crossfade_reduction=-3922.52` with `repeat=2`. The metric ignores repeat — true reduction is `original×repeat − final = 31.20s`. Analysis flags the symptom but does not specify the fix or whether it blocks PASS. | Plan should include explicit fix: `crossfade_reduction = total_original_duration * repeat - final_duration`. Also note this is a stored value in already-written reports — decide whether to backfill or leave historical. |
| Medium | analysis §Engineering Lead bullet "pack은 output artifact writer return value를 확인하지 않아" | Verified at `wavvy.py:2410-2413`: `generate_provenance`, `generate_upload_csv`, `generate_report` are called bare — no return check, no exception trap. PACK COMPLETE banner prints regardless. Analysis is correct; severity is higher than implied because this corrupts the very artifacts the proposed gate will read. | Treat this as a P0 fix that must precede `gate` work — otherwise the gate trusts artifacts that may be silently truncated/missing. Add to MVP scope. |
| Medium | analysis §Engineering Lead bullet "spec drift" | Verified: `wavvy.py:445` writes `final.mkv`; docstring/banner at `:2128`, `:2198` say `final.mp4`; banner says `=== VIBEM ===` (lines 1929/2004/2200/2464/2595/2627). CLAUDE.md says project name is Wavvy. Analysis correctly flags drift but does not assign cleanup ownership. | Plan must decide: rename to Wavvy banner + commit to `final.mkv` everywhere (or vice versa). This is cheap and removes recurring confusion for downstream agents. |
| Medium | analysis §AI Ops + §Recommendation #5 (peer-agent-review reuse) | `peer-agent-review.sh` lives in `~/Project/claude-center` (cross-repo). Treating it as a Wavvy harness API creates an undeclared external dependency — if claude-center moves or the project is cloned to another machine, gate breaks silently. | `wavvy.py doctor` should explicitly probe for `claude-center/scripts/peer-agent-review.sh` and surface a clear error. Or vendor a shim. Analysis does not flag this coupling. |
| Medium | analysis §QA Reviewer "check_series_gate.sh fails for FINAL" | Verified: `MASTER/scripts/check_series_gate.sh:104` only collects `*.txt`; `SERIES/20-00/input/tracks/` now contains only renamed `*.wav` files (txt was archived in finalize-upload flow). So the script exits with `ERROR: 트랙 파일 없음` for any FINAL series. Correct observation, but analysis does not propose where the rubric snapshot goes after txt archival. | Plan should specify: rubric must run pre-finalize and the result snapshot stored (e.g. in `report.json` or a new `rubric_snapshot.json`) so post-finalize gate can verify historical pass without txt. |
| Medium | analysis §Recommendation #2 (SSOT) | A `MASTER/SSOT.md` doc with no enforcement mechanism becomes another stale doc — exactly the symptom the analysis warns about for `concept.md` (line 143 verified: `12 PASS + 8 draft` despite Final Track Sources being present). | Either drop SSOT.md and bake conflict-order into `wavvy.py state`'s output, or add a CI/pre-commit check that fails if listed files mention each other inconsistently. Documentation alone will rot. |
| Medium | analysis §Recommendation #3 ("doctor/state/gate" all in wavvy.py) | `wavvy.py` is already 2,891 lines (verified). Adding three new top-level commands without first introducing a CLI dispatcher pushes toward a 4,000+ line monolith. Analysis defers refactor "to reduce regression risk" but does not budget for the cognitive cost of the larger file. | Plan should at least split commands into `wavvy/cli/*.py` modules from the start so each new command lands in its own file. The refactor cost is low when the new code is being added anyway; deferring makes the eventual split harder. |
| Medium | analysis §Recommendation #5 (cross-project pipeline) | "Artifact-mode adapter for /team and /director" and `.ai/pipeline/runs/YYYYMMDD-HHMMSS_<slug>/` are named but not specified. Stage list has no inputs/outputs/exit codes. Without a concrete contract this slot will be re-debated at planning time. | Either remove this from MVP scope, or specify each stage's input artifacts, output artifacts, and pass/fail signal in the plan doc. As written, this is the most underspecified part of the analysis. |
| Low | analysis §Engineering Lead bullet "preview는 image mode validation을 통과해도 render 단계에서 `paths.loop_video`를 사용해 실패할 가능성" | I did not verify this end-to-end. Plausible from the code path but the analysis doesn't cite a line. | Before treating as finding, cite the offending line in `preview` or downgrade to "needs verification". |
| Low | analysis §Product Leader | Success metric ("다른 에이전트가 `-wavvy` 후 `wavvy.py state SERIES/20-00 --check`만 보고 다음 행동을 정확히 말할 수 있는 것") is good but unmeasurable. | Define an evaluation harness: e.g. capture 3-5 known-state series snapshots and assert `state --check` output matches expected next-action strings. This is the "capability/regression suite" the AI Ops section says is missing — but it's missing from the MVP recommendation too. |
| Low | analysis §QA Reviewer "tools/에는 실질 도구가 없고 .DS_Store만 있다" | Verified (only `tools/.DS_Store`). Trivial; not on critical path. | Either delete `tools/` or document its intended use. |

### Test And Acceptance Notes
- Verified by direct file inspection: `wavvy.py` line count (2,891), VIBEM banners, `final.mkv`/`final.mp4` drift, `provenance/upload/report` writer return values not checked, `report.json` `crossfade_reduction=-3922.52`, `concept.md` `12 PASS + 8 draft` line, `tools/` contents, `MASTER/scripts/check_series_gate.sh` `*.txt` requirement, `output/` artifacts present (no `final.mkv`/`upload.csv`).
- Did not run `check_series_gate.sh` (read-only review); claim that it fails is consistent with the script's `find ... -name "*.txt"` and the current `input/tracks/` containing only `.wav`.
- Did not verify the `preview` image-mode regression claim — analysis should cite a specific line.
- No tests were added; the analysis is a planning artifact.

### Follow-Up For Requesting Agent
1. Resolve the FINAL-state semantics question (Source-Final vs Render-Final vs Upload-Final) and bake the phase enum into `.ai/state.json` schema before writing the plan. This is the single most important open question.
2. Decide HANDOFF.md migration: keep + auto-generate from state.json, or update the SessionStart/UserPromptSubmit hooks in `~/Project/claude-center` first. Do not demote HANDOFF without that hook update.
3. Promote two analysis findings into MVP scope (they're code bugs, not new harness work): (a) check return values of `generate_provenance/upload_csv/report` in `wavvy.py:2410-2413`, (b) fix `crossfade_reduction` math to account for `repeat`. Both will silently corrupt any gate that reads `report.json`.
4. Concretize or drop §Recommendation #5 — specify stages' inputs/outputs/exit codes, or defer to a follow-up doc.
5. Add a single-writer + atomic-write rule for `.ai/state.json` and name the writer (`wavvy.py state`).
6. Either move `doctor/state/gate` into a `wavvy/cli/*.py` package from day one, or explicitly accept the 4k-line monolith trajectory.
7. Cite the line for the `preview` image-mode claim, or downgrade it to a hypothesis.

When these are resolved, I'd expect the next iteration to PASS without rework.

